# Some Scripts

# alog.pyw:
  runs on Windows with Python3. 
  
  Packages needed:
  
    time
    requests
    threading
    tkinter
    json
  
# alog.command:
  runs on OS X or Linux
  
  (so many bugs, need someone help me fixed it)
